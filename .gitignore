node_modules/
dist/
data/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Sources brutes du pack Pixel Crawler (payant) : les zips et l'arborescence
# d'origine restent hors dépôt — seules les feuilles normalisées utilisées par
# le jeu (public/assets/pack/) sont versionnées, dépôt privé oblige.
assets_src/
*.zip
